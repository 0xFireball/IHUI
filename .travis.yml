language: node
script: npm run mk
before_install:
  - npm i -g node-sass minifier
deploy:
  provider: pages
  skip_cleanup: true
  github_token: $GITHUB_TOKEN # Set in travis-ci.org dashboard
  on:
    branch: master